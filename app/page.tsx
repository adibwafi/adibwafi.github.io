import HomePage from '@/components/HomePage';

/* ─── Home route — / ─────────────────────────────────────────────────────── */
// Metadata is inherited from app/layout.tsx (root metadata covers this page)

export default function Page() {
  return <HomePage />;
}
